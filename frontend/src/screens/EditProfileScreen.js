import React,{useState}from'react';import{Alert,ScrollView,StyleSheet,TextInput,View}from'react-native';import ScreenHeader from'../components/ScreenHeader';import AppText from'../components/AppText';import PrimaryButton from'../components/PrimaryButton';import{useApp}from'../context/AppContext';import{colors,radius,spacing}from'../theme';
export default function EditProfileScreen({navigation}){const{user,updateProfile}=useApp();const[name,setName]=useState(user?.fullName||'');const[loading,setLoading]=useState(false);async function save(){if(name.trim().length<2)return Alert.alert('Name required','Enter at least two characters.');setLoading(true);try{await updateProfile(name.trim());navigation.goBack();}catch(e){Alert.alert('Update failed',e.message);}finally{setLoading(false);}}return <View style={styles.page}><ScreenHeader title="Edit profile" subtitle="Update your traveler identity" onBack={()=>navigation.goBack()}/><ScrollView contentContainerStyle={styles.content}><AppText variant="caption" color={colors.muted}>FULL NAME</AppText><TextInput accessibilityLabel="Full name" style={styles.input} value={name} onChangeText={setName} maxLength={80} autoCapitalize="words"/><AppText variant="caption" color={colors.muted}>EMAIL</AppText><View style={styles.readonly}><AppText>{user?.email}</AppText></View><AppText variant="caption" color={colors.muted}>Email changes are disabled in this demonstration to preserve account identity.</AppText><PrimaryButton title="SAVE PROFILE" onPress={save} loading={loading}/></ScrollView></View>}
const styles=StyleSheet.create({page:{flex:1,backgroundColor:colors.cream},content:{padding:spacing.lg,gap:spacing.sm},input:{minHeight:50,borderWidth:1,borderColor:colors.line,borderRadius:radius.md,backgroundColor:colors.paper,paddingHorizontal:14,color:colors.ink,marginBottom:8},readonly:{minHeight:50,borderRadius:radius.md,backgroundColor:colors.line,paddingHorizontal:14,justifyContent:'center'}});
