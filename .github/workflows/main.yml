name: Smart Travel Planner CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Backend Install
      - name: Install Backend Dependencies
        run: |
          cd backend
          npm install

      # Frontend Install
      - name: Install Frontend Dependencies
        run: |
          cd frontend
          npm install